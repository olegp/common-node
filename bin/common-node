#!/bin/bash
NODE=`which node`
COMMON_PATH=`echo 'console.log(require("path").dirname(require.resolve("common-node")))' | $NODE | head -n1 | egrep -o '/[^'\'']+'`
export NODE_PATH="$NODE_PATH:$COMMON_PATH"
exec node $COMMON_PATH/run.js "$@"
